# api/.platform/nginx/conf.d/

Nginx configuration for the Elastic Beanstalk platform layer.

## Files

- **websocket.conf** -- Configures Nginx to proxy WebSocket connections (`Upgrade` and `Connection` headers) for the Yjs collaboration server.
