# shared/

TypeScript type definitions shared between the API and web packages.

## Files

- **package.json** -- Package config for the `@ship/shared` workspace package.
- **tsconfig.json** -- TypeScript configuration.

## Directories

- **src/** -- Source code with shared types and constants.
